# SPDX-License-Identifier: MIT OR Apache-2.0
#
# `otp_variant` chooses between the static and the dynamic build of a musl
# runtime, and the catalogue publishes exactly those two.

name = "bad_variant_app"
version = "0.1.0"

[tools.ginary.target.linux-x86_64-musl]
otp_variant = "hybrid"
